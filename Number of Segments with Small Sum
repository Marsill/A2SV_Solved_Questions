n, s = map(int, input().split())
num = list(map(int, input().split()))

window = 0
left = 0
max_seg = 0

for right in range(len(num)):
    window += num[right]
    while window > s:
        window -= num[left]
        left += 1

    max_seg += right - left + 1
print(max_seg)
