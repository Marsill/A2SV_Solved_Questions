class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        n = len(nums)
        result = []
        freq = Counter(nums)
        for k, v in freq.items():
            if v > n/3:
                result.append(k)
        
        return result
