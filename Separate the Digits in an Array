class Solution:
    def separateDigits(self, nums: List[int]) -> List[int]:
        res = []
        for num in nums:
            for n in str(num):
                res.append(int(n))
        return res
