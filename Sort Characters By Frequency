class Solution:
    def frequencySort(self, s: str) -> str:
        count = Counter(s)

        sorted_dict = sorted(count.items(), key=lambda x: (-x[1], x[0]))

        result = ''.join(ch * freq for ch, freq in sorted_dict)
        
        return result
