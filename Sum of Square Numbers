class Solution:
    def judgeSquareSum(self, c: int) -> bool:
        a = 0
        b = int(sqrt(c))
        while a <= b:
            square = a*a + b*b
            if square == c:
                return True
            elif square > c:
                b -= 1
            else:
                a += 1
        return False
